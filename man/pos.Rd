% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-pos.R
\name{pos}
\alias{pos}
\title{Position vectors}
\usage{
pos(x = numeric(), y = x)
}
\arguments{
\item{x}{Numeric vector of x-coordinates. If \code{y} is missing,
both x and y coordinates are set to \code{x}.}

\item{y}{Numeric vector of y-coordinates. Defaults to \code{x}.}
}
\value{
An object of class \code{pos} with recycled \code{x} and \code{y} components.
}
\description{
\code{pos()} creates a vector of (x, y) positions. Positions support
arithmetic operations with other positions and numeric scalars.
}
\details{
Position objects are built on the \strong{vctrs} infrastructure and support:
\itemize{
\item Field access: \code{pos$x} and \code{pos$y}
\item Arithmetic: \code{+}, \code{-}, \code{*}, \code{/}, etc. (vectorized)
\item Math functions: \code{sum()}, \code{mean()}, \code{prod()} on positions
}

Use \code{positions(...)} to combine multiple \code{pos} objects into a
single vector.
}
\examples{
# Create individual positions
p1 <- pos(1, 2)
p2 <- pos(3, 4)

# Access fields
p1$x
p1$y

# Arithmetic
p1 + p2
p1 - p2
p1 * 2

# Combine positions
positions(p1, p2)

}
