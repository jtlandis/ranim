% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape-rect.R
\name{calc_grid}
\alias{calc_grid}
\title{Calculate grid positions within an object}
\usage{
calc_grid(
  obj,
  ncol = 1,
  nrow = 1,
  padding = 0.05,
  xpadding = padding,
  ypadding = padding,
  margin = 0.5,
  xmargin = margin,
  ymargin = margin
)
}
\arguments{
\item{obj}{A \code{\link{shape}} object whose extent is used to define the grid.}

\item{ncol}{Number of columns in the grid (default 1).}

\item{nrow}{Number of rows in the grid (default 1).}

\item{padding}{Padding as a fraction of available space (default 0.05).
Applied uniformly unless overridden by \code{xpadding}/\code{ypadding}.}

\item{xpadding}{Padding in x direction as a fraction of space.}

\item{ypadding}{Padding in y direction as a fraction of space.}

\item{margin}{Margin outside the grid as a fraction of padding
(default 0.5). Applied uniformly unless overridden by \code{xmargin}/\code{ymargin}.}

\item{xmargin}{Margin in x direction.}

\item{ymargin}{Margin in y direction.}
}
\value{
A list of \code{\link{pos}} vectors, one per column, where each vector
contains the positions of all cells in that column. An attribute
\code{"features"} contains the computed cell dimensions and spacing.
}
\description{
\code{calc_grid()} computes the positions for a regular grid of cells
within an object's bounding box. Useful for laying out grids of
shapes with uniform spacing.
}
\details{
The grid is computed as follows:
\enumerate{
\item The object's extent is determined via \code{\link[=get_extent]{get_extent()}}.
\item Total space is divided by padding to yield the grid cell size.
\item Cell positions are computed column-by-column.
}
}
\seealso{
\code{\link[=spawn_grid]{spawn_grid()}}, \code{\link[=get_extent]{get_extent()}}, \code{\link[=get_positions]{get_positions()}}
}
\keyword{internal}
