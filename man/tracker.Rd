% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-tracker.R
\name{tracker}
\alias{tracker}
\title{Tracker objects}
\usage{
tracker(object, tracked = list())
}
\arguments{
\item{object}{A \code{\link{shape}} object (typically a \code{\link[=window]{window()}}) to track
children within.}

\item{tracked}{Optional named list of shapes to track initially.}
}
\value{
An object of class \code{tracker}.
}
\description{
A \code{tracker} manages references to specific shapes within a hierarchy,
preserving those references when the hierarchy is cloned. This is
essential for working with complex shape trees where you need to
maintain access to particular child shapes across multiple animation
iterations.
}
\details{
A \code{tracker} is useful when you have a complex shape hierarchy and want
to save checkpoints of the animation state. By tracking specific shapes
(e.g., the main grid, rows, columns), you can restore them later via
\verb{$clone()} and \verb{$restore()} without losing references.

Example workflow:
\enumerate{
\item Create a window and some shapes.
\item Use \code{tracker$track(shape1 = my_shape, shape2 = another)} to register them.
\item Add animations.
\item Save state with \code{checkpoint <- tracker$clone()}.
\item Continue animating.
\item Restore with \code{checkpoint$restore()} to get back the original shapes.
}
}
\section{Properties}{

\itemize{
\item \code{object}: The root \code{\link{shape}} being tracked.
\item \code{tracked}: A named list of \code{\link{shape}} objects to keep track of.
\item \code{find(name)}: Function to locate a tracked shape by name.
\item \code{track(...)}: Function to register new shapes to track.
\item \code{clone()}: Function to create a deep copy of the tracker with all
references updated.
\item \code{restore()}: Function to clone and restore tracked shapes to the
calling environment as variables.
}
}

\section{Constructor}{

\code{tracker(object, tracked = list())}
}

\examples{
w <- window(bl = 0, tr = pos(10, 10))
rect_obj <- rect(width = 2, height = 2, trans = transform(pos(5, 5)))
w@child(rect_obj)

# Create a tracker
trk <- tracker(w)
trk@track(my_rect = rect_obj)

# Add animations
rect_obj@action(translate(to = pos(8, 8), time = time(2)))

# Save state
checkpoint <- trk@clone()

# Continue animating
rect_obj@action(color("red", "blue", time = time(1)))

# Restore to checkpoint
restored_trk <- checkpoint@restore()

}
\seealso{
\code{\link[=obj_clone]{obj_clone()}}, \link{shape}, \code{\link[=window]{window()}}
}
