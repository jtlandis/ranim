% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape-line.R
\name{line}
\alias{line}
\title{Line shape}
\usage{
line(
  ...,
  trans = transform(),
  parent = NULL,
  children = list(),
  actions = list(),
  color = "black",
  weight = -1,
  arrow = NULL,
  stroke = "solid"
)
}
\arguments{
\item{...}{Positions (as \code{\link{pos}} objects or coordinates) defining the line path.}

\item{trans}{A \code{\link{transform}} object giving position, size, and angle.
Defaults to \code{transform()}.}

\item{parent}{An optional parent \code{\link{shape}}.}

\item{children}{Optional list of child \code{\link{shape}} objects.}

\item{actions}{Optional list of \code{\link{action}} objects to apply.}

\item{color}{A color specification (see \code{\link{class_color}}). Default \code{"black"}.}

\item{weight}{Numeric controlling curve smoothness. -1 (default) means straight lines;
values in \[0, 1\] create curved lines.}

\item{arrow}{Optional \code{\link{arrow}} object for arrow decoration.}

\item{stroke}{Line type: \code{"solid"}, \code{"dashed"}, \code{"dotted"}, etc.
Default \code{"solid"}.}
}
\value{
An object of class \code{line} (inherits from \code{\link{shape}}).
}
\description{
\code{line()} creates a line shape connecting multiple points. Lines can be
straight or curved (via \code{weight}), and can have arrows at one or both ends.
}
\examples{
# Straight line from (1, 1) to (9, 9)
l <- line(
  pos(1, 1), pos(9, 9),
  color = "blue",
  trans = transform()
)

# Curved line with arrow at end
l <- line(
  pos(0, 0), pos(5, 5), pos(10, 0),
  weight = 0.5,
  arrow = arrow(side = "end")
)

}
\seealso{
\code{\link[=rect]{rect()}}, \code{\link[=point]{point()}}, \code{\link[=polygon]{polygon()}}, \code{\link{shape}}
}
