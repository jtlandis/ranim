% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gif.R
\name{render_gif}
\alias{render_gif}
\title{Render an animation as a GIF}
\usage{
render_gif(
  obj,
  file,
  fps = 100,
  keep_frames = FALSE,
  quite = FALSE,
  width = 6,
  height = 4,
  units = "in",
  dpi = 100,
  loop = 0,
  clone = TRUE,
  calc = FALSE
)
}
\arguments{
\item{obj}{A \code{\link{shape}} object to animate.}

\item{file}{Output file path for the GIF.}

\item{fps}{Frames per second for the output GIF (and for internal
rendering if \code{obj} uses FPS-based timing).}

\item{keep_frames}{If \code{TRUE}, keep the intermediate PNG frames
in a temporary directory; otherwise they are deleted after
creating the GIF.}

\item{quite}{Logical; if \code{FALSE} (default), progress is printed
to the console. (Note: argument is spelled \code{quite} for
historical reasons and may be renamed in future versions.)}

\item{width, height}{Size of the graphics device. Default 6 inches
by 4 inches.}

\item{units}{Units for \code{width} and \code{height}. Can be \code{"in"} (inches),
\code{"px"} (pixels), \code{"cm"}, or \code{"mm"}. Passed to \code{\link[grDevices:png]{grDevices::png()}}.}

\item{dpi}{Resolution in dots per inch, used when \code{units} is not
\code{"px"}. Default 100.}

\item{loop}{Number of times the GIF should loop. \code{0} means loop
infinitely; pass a positive integer to loop that many times.}

\item{clone}{If \code{TRUE} (default), work on a deep copy of \code{obj};
this preserves the original object state. If \code{FALSE}, \code{obj} is
modified in place.}

\item{calc}{If \code{TRUE}, first compute the exact duration of the
animation via \code{\link[=calc_duration]{calc_duration()}} and preallocate the correct
number of frames. If \code{FALSE} (default), render up to a maximum
of 1000 frames.}
}
\value{
The animated \code{shape} (possibly a clone), invisibly.
}
\description{
\code{render_gif()} advances an animated \code{\link{shape}} over time, rendering
each frame to a PNG, and then combines the frames into an animated
GIF using the \strong{magick} package.
}
\details{
The function creates a temporary directory, renders each frame
of the animation as a PNG file, then uses \strong{magick} to combine
them into a GIF. On exit, temporary PNG files are deleted
(unless \code{keep_frames = TRUE}).
}
\examples{
\dontrun{
# Create a simple animation
w <- window(bl = 0, tr = pos(10, 10))
w@action(translate(to = pos(5, 5), time = time(2)))

# Render to GIF
render_gif(w, "animation.gif", fps = 25, width = 8, height = 6)
}

}
\seealso{
\code{\link[=anim]{anim()}}, \code{\link[=render]{render()}}
}
