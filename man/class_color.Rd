% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-color.R
\name{class_color}
\alias{class_color}
\title{Color objects}
\usage{
class_color(color)
}
\arguments{
\item{color}{A color specification. Can be:
\itemize{
\item A character string: hex code (starting with \code{"#"}) or named color
\item A numeric vector of length 3 (RGB, values 0-255)
\item A numeric vector of length 4 (RGBA, values 0-255)
}}
}
\value{
An object of class \code{color} (inherits from character).
}
\description{
\code{class_color()} creates color objects that are validated and can be
used throughout the animation system. Colors can be specified as
hex strings (e.g., \code{"#FF0000"}), R color names (e.g., \code{"red"}),
or as numeric RGB(A) vectors.
}
\details{
Colors are validated to ensure they are either valid hex codes
or recognized R color names. Numeric inputs are converted to
hex via \code{\link[grDevices:rgb]{grDevices::rgb()}}.
}
\examples{
# Named color
class_color("red")

# Hex color
class_color("#FF0000")

# RGB numeric (0-255)
class_color(c(255, 0, 0))

# RGBA numeric
class_color(c(255, 0, 0, 128))

}
