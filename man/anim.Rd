% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{anim}
\alias{anim}
\title{Animate a shape hierarchy over time}
\usage{
anim(obj, fps = NULL)
}
\arguments{
\item{obj}{A \code{\link{shape}} object to animate.}

\item{fps}{Frames per second. If \code{NULL} (the default), a
wall-clock based \code{timed_clock} is used and frame duration
depends on rendering speed. If numeric, uses a \code{simple_clock}
with fixed time steps.}
}
\value{
A cloned copy of \code{obj} in its final state.
}
\description{
\code{anim()} repeatedly advances the animation state of a \code{\link{shape}}
using its \verb{@act} method and calls \code{\link[=render]{render()}} on each frame.
It is mainly useful for interactive, on-screen animation. For
saving animations as GIF files, see \code{\link[=render_gif]{render_gif()}}.
}
\seealso{
\code{\link[=render]{render()}}, \code{\link[=render_gif]{render_gif()}}, \code{\link{shape}}
}
