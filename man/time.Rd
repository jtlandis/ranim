% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-time.R
\name{time}
\alias{time}
\title{Time objects for animation control}
\usage{
time(duration = 1, ease = identity, repeating = 0, mode = "time")
}
\arguments{
\item{duration}{Numeric duration of the animation in seconds.
Defaults to 1.}

\item{ease}{An easing function (see \code{\link[=ease]{ease()}}) that maps time
from \[0, 1\] to a progress value in \[0, 1\]. Defaults to \code{\link{elinear}}.}

\item{repeating}{Integer: number of times to repeat the animation
after the first playthrough (0 = play once, 1 = play twice, etc.).
Can also be \code{Inf} for infinite repetition.}

\item{mode}{Timing mode: \code{"time"} (default) for real-time updates or
\code{"fps"} for fixed frame rate. Usually set via \code{\link[=window]{window()}}.}
}
\value{
An object of class \code{time}.
}
\description{
\code{time()} creates a time object that controls the duration, pacing,
and repetition of \code{\link{action}} objects. Times are tied to easing
functions that determine how values interpolate over the duration.
}
\section{Properties}{

\itemize{
\item \code{time}: Current phase in \[0, 1\].
\item \code{value}: Current eased value in \[0, 1\].
\item \code{delta}: Change in value since the last step.
\item \code{iter}: Current iteration count (0-indexed).
\item \code{is_done}: Logical; whether all repetitions are complete.
\item \code{left}: Remaining time in current cycle.
\item \code{remaining}: Total remaining time including all remaining cycles.
\item \code{duration}: Total duration of one cycle.
\item \code{step(delta_time)}: Advance time by \code{delta_time} units.
\item \code{reset()}: Reset to initial state.
}
}

\examples{
# Simple 1-second animation
t <- time(duration = 1)

# 2-second animation that repeats 3 times (4 total playthroughs)
t <- time(duration = 2, repeating = 3)

# With cubic easing
t <- time(duration = 1, ease = ease(ein = ecubic))

}
