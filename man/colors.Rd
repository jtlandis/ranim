% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util-actions.R
\name{colors}
\alias{colors}
\title{Multiple color transitions}
\usage{
colors(colors, durations, repeating = 0)
}
\arguments{
\item{colors}{A list or character vector of colors (any format
accepted by \code{\link{class_color}}).}

\item{durations}{Either a single numeric duration (applied to all
transitions), a \code{\link{time}} object (cloned for each transition),
or a list of \code{\link{time}} objects (one per transition).}

\item{repeating}{Number of times to repeat the entire color sequence
after the first playthrough.}
}
\value{
An \code{\link{act_series}} object executing all color transitions in sequence.
}
\description{
\code{colors()} creates an action sequence that transitions through
a list of colors, visiting each in sequence. Useful for
multi-step color animations.
}
\examples{
w <- window(bl = 0, tr = pos(10, 10))
rect_obj <- rect(width = 2, height = 2, trans = transform(pos(5, 5)))
w@child(rect_obj)

# Cycle through red -> green -> blue, each taking 0.5 seconds
rect_obj@action(colors(c("red", "green", "blue"), durations = 0.5))

}
\seealso{
\code{\link[=color]{color()}}, \code{\link{act_series}}
}
