% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-shape.R
\name{shape}
\alias{shape}
\title{Shape objects}
\usage{
shape(
  trans = transform(),
  parent = NULL,
  children = list(),
  actions = list(),
  color = "black"
)
}
\arguments{
\item{trans}{a \link{transform} giving the local/global position,
size, and anchor.}

\item{parent}{another \code{shape} or \code{NULL}.}

\item{children}{a list of child shapes.}

\item{actions}{a list of \link{action} objects to apply to this shape.}

\item{color}{a \link{class_color} object used by rendering methods.}
}
\description{
A \code{shape} is the fundamental unit of the animation system. Shapes
can contain other shapes (children), have a position and size
(through a \link{transform} object), and carry a list of time-varying
\link{action} objects that drive animation.

Shapes form a tree: each shape has at most one parent, and
zero or more children. Transformations are anchored at the
parent's global position, so moving or scaling a parent
implicitly affects all of its descendants.
}
\section{Properties}{

\itemize{
\item \code{trans}: a \link{transform} giving the local/global position,
size, and anchor.
\item \code{parent}: another \code{shape} or \code{NULL}.
\item \code{children}: a list of child shapes.
\item \code{global}: computed position in world coordinates.
\item \code{actions}: a list of \link{action} objects to apply to this shape.
\item \code{action}: function to attach new actions to this shape.
\item \code{act}: function to advance animation state.
\item \code{color}: a \link{class_color} object used by rendering methods.
\item \code{size}: numeric size (derived from transform).
}
}

\section{Constructor}{

\code{shape(trans = transform(), parent = NULL, children = list(), actions = list(), color = "black")}

Most users will not construct bare \code{shape} objects directly;
instead they work with concrete shapes like \code{\link[=rect]{rect()}}, \code{\link[=point]{point()}},
\code{\link[=polygon]{polygon()}}, \code{\link[=text]{text()}}, etc., which all extend \code{shape}.
}

