% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util-actions.R
\name{spawn_grid}
\alias{spawn_grid}
\title{Spawn a grid of shapes}
\usage{
spawn_grid(
  nrow = 1,
  ncol = 1,
  padding = 0.05,
  color = "black",
  apply = NULL,
  actions = list(),
  spawn_time = 0,
  xtime = 0.1,
  ytime = 0.1,
  time = 0,
  xpadding = padding,
  ypadding = padding
)
}
\arguments{
\item{nrow}{Number of rows in the grid.}

\item{ncol}{Number of columns in the grid.}

\item{padding}{Padding as a fraction of available space (default 0.05).
Applied uniformly; see \code{xpadding} and \code{ypadding} for axis-specific control.}

\item{color}{Color of each grid cell.}

\item{apply}{An optional function \verb{function(cell)} applied to each
newly created cell before animation.}

\item{actions}{A list of \code{\link{action}} objects to attach to each cell.}

\item{spawn_time}{Time delay before spawning each cell (default 0).}

\item{xtime}{Time to stagger cells horizontally (default 0.1).}

\item{ytime}{Time to stagger cells vertically (default 0.1).}

\item{time}{A \code{\link{time}} object for the overall spawning action
(default instant, \code{time(0)}).}

\item{xpadding}{Padding in x direction as a fraction of available space.}

\item{ypadding}{Padding in y direction as a fraction of available space.}
}
\value{
An \code{\link{action}} object.
}
\description{
\code{spawn_grid()} creates an action that generates a grid of child
shapes with optional spacing, padding, and individual actions.
Each cell is spawned with staggered timing for visual effect.
}
\details{
The grid spawning uses a nested spawning pattern: horizontal stalks
are created, and within each stalk, cells are spawned vertically.
This creates a visual sweep effect when \code{xtime} and \code{ytime} are nonzero.
}
\seealso{
\code{\link[=spawn]{spawn()}}, \code{\link[=spawn_from]{spawn_from()}}, \code{\link[=rect]{rect()}}
}
