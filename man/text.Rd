% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape-text.R
\name{text}
\alias{text}
\title{Text shape}
\usage{
text(
  label,
  trans = transform(),
  parent = NULL,
  children = list(),
  actions = list(),
  color = "black",
  hadj = 0.5,
  vadj = 0.5,
  font = "plain"
)
}
\arguments{
\item{label}{Character string; the text to display.}

\item{trans}{A \code{\link{transform}} object giving position, size, and angle.
Defaults to \code{transform()} (origin, size 1, no rotation).
\itemize{
\item \code{trans@size} controls the character expansion (\code{cex})
\item \code{trans@angle} controls text rotation (\code{srt})
\item \code{trans@global} specifies the (x, y) position
}}

\item{parent}{An optional parent \code{\link{shape}}. If provided, this shape
becomes a child of the parent.}

\item{children}{Optional list of child \code{\link{shape}} objects.}

\item{actions}{Optional list of \code{\link{action}} objects to apply.}

\item{color}{A color specification (see \code{\link{class_color}}). Default \code{"black"}.}

\item{hadj}{Horizontal text justification; 0 = left, 0.5 = center, 1 = right.
Default 0.5 (centered).}

\item{vadj}{Vertical text justification; 0 = bottom, 0.5 = center, 1 = top.
Default 0.5 (centered).}

\item{font}{Font style: one of \code{"plain"}, \code{"bold"}, \code{"italic"}, \code{"emphasis"}.
Default \code{"plain"}.}
}
\value{
An object of class \code{text} (inherits from \code{\link{shape}}).
}
\description{
\code{text()} creates a text shape object. Text is rendered as labels at
a specified position, with control over font, size, and color.
}
\section{Properties}{

\itemize{
\item \code{label}: Character string; the text content.
\item \code{hadj}: Horizontal adjustment (0-1).
\item \code{vadj}: Vertical adjustment (0-1).
\item \code{font}: Font style.
\item All properties inherited from \code{\link{shape}}: \code{trans}, \code{parent},
\code{children}, \code{actions}, \code{color}, etc.
}
}

\examples{
# Create text at (5, 5)
t <- text(
  label = "Hello",
  color = "blue",
  trans = transform(pos(5, 5))
)

# Larger, bold text
t <- text(
  label = "World",
  font = "bold",
  trans = transform(pos(5, 5), size = 2)
)

}
\seealso{
\code{\link[=rect]{rect()}}, \code{\link[=point]{point()}}, \code{\link[=polygon]{polygon()}}, \code{\link{shape}}
}
