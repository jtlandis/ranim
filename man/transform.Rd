% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-transform.R
\name{transform}
\alias{transform}
\title{Transform objects}
\usage{
transform(offset = pos(0), anchor = pos(0))
}
\description{
A \code{transform} encapsulates the spatial properties of a \link{shape}:
position, size, and rotation angle. Transforms use an anchor point
(typically the parent's position) and an offset to compute the
global position.
}
\section{Properties}{

\itemize{
\item \code{anchor}: A \code{\link{pos}} object marking the reference point
(typically inherited from the parent shape).
\item \code{offset}: A \code{\link{pos}} object giving the local displacement
relative to the anchor.
\item \code{global}: Computed property; the sum of \code{anchor} and \code{offset}.
Can be assigned to directly; assignment updates \code{offset}.
\item \code{size}: Numeric scale factor for the shape (default 1).
\item \code{angle}: Numeric rotation angle in degrees (default 0).
}
}

\section{Constructor}{

\code{transform(offset = pos(0), anchor = pos(0))}
}

