% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-window.R
\name{window}
\alias{window}
\title{Window objects}
\usage{
window(
  bl,
  tr,
  trans = transform(),
  parent = NULL,
  children = list(),
  actions = list(),
  color = "#ffffff00",
  fps = NULL
)
}
\arguments{
\item{bl}{A \code{\link{pos}} for the bottom-left corner of the window.}

\item{tr}{A \code{\link{pos}} for the top-right corner of the window.}

\item{trans}{A \code{\link{transform}} object. Defaults to \code{transform()}.}

\item{parent}{An optional parent \link{shape}}

\item{children}{Optional list of child \link{shape} objects.}

\item{actions}{Optional list of \code{\link{action}} objects to apply.}

\item{color}{A color specification. Default is transparent white.}

\item{fps}{Frames per second for animation. If \code{NULL} (default),
uses real-time mode. If numeric, uses fixed frame timing.}
}
\value{
An object of class \code{window} (inherits from \link{shape}).
}
\description{
A \code{window} is a special \code{\link{shape}} that defines the canvas for animation.
It has a bounding box (via \code{bl} and \code{tr} for bottom-left and top-right),
and optional FPS settings that apply to all children.
}
\section{Properties}{

\itemize{
\item \code{bl}: A \code{\link{pos}} for the bottom-left corner.
\item \code{tr}: A \code{\link{pos}} for the top-right corner.
\item \code{fps}: Frames per second for all actions. If \code{NULL}, uses real-time
mode; if numeric, sets fixed frame timing.
\item All properties inherited from \link{shape}: \code{trans}, \code{parent},
\code{children}, \code{actions}, \code{color}, etc.
}
}

\section{Constructor}{

\code{window(bl, tr, trans = transform(), parent = NULL, children = list(), actions = list(), color = "#ffffff00", fps = NULL)}
}

\examples{
# Create a 10x5 window
w <- window(bl = pos(0, 0), tr = pos(10, 5))

# With FPS set to 25
w <- window(bl = 0, tr = pos(10, 5), fps = 25)

}
\seealso{
\link{shape}, \code{\link[=rect]{rect()}}, \code{\link[=anim]{anim()}}, \code{\link[=render_gif]{render_gif()}}
}
