% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape-point.R
\name{point}
\alias{point}
\title{Point shape}
\usage{
point(
  trans = transform(),
  parent = NULL,
  children = list(),
  actions = list(),
  color = "black"
)
}
\arguments{
\item{trans}{A \code{\link{transform}} object giving position, size, and angle.
Defaults to \code{transform()} (origin, size 1, no rotation).}

\item{parent}{An optional parent \code{\link{shape}}. If provided, this shape
becomes a child of the parent.}

\item{children}{Optional list of child \code{\link{shape}} objects.}

\item{actions}{Optional list of \code{\link{action}} objects to apply.}

\item{color}{A color specification (see \code{\link{class_color}}). Default \code{"black"}.}
}
\value{
An object of class \code{point} (inherits from \code{\link{shape}}).
}
\description{
\code{point()} creates a point (dot) shape object. Points are rendered as
small circles at their position, with size controlled by the \code{size}
property of the shape's transform.
}
\details{
Points are rendered as single points on a plot. The \code{size} property
controls the point size (via the \code{cex} parameter in \code{plot.xy()}).
}
\examples{
# Create a blue point at (5, 5)
p <- point(
  color = "blue",
  trans = transform(pos(5, 5))
)

# Create a larger point
p <- point(color = "red", trans = transform(pos(5, 5), size = 2))

}
\seealso{
\code{\link[=rect]{rect()}}, \code{\link[=polygon]{polygon()}}, \code{\link[=text]{text()}}, \code{\link{shape}}
}
