% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util-ease.R
\name{ease}
\alias{ease}
\title{Composite ease functions}
\usage{
ease(ein = NULL, eout = NULL, etime = elinear, mode = c("default", "weighted"))
}
\arguments{
\item{ein}{An optional ease-in function (applied to the first half
of the interval in default mode). See \code{\link[=elinear]{elinear()}} and related
easing functions for valid examples.}

\item{eout}{An optional ease-out function (applied to the second half
of the interval in default mode). See \code{\link[=elinear]{elinear()}} and related
easing functions for valid examples.}

\item{etime}{A base time transformation applied to the entire interval.
Defaults to \code{\link[=elinear]{elinear()}}.}

\item{mode}{How to combine \code{ein} and \code{eout}:
\itemize{
\item \code{"default"}: Apply \code{ein} to the sub-interval \[0, 0.5) and \code{eout}
to the sub-interval \[0.5, 1\]
\item \code{"weighted"}: Blend \code{ein} and \code{eout} based on position in \[0, 1\]
}}
}
\value{
An object of class \code{ease} that can be used with \code{\link[=time]{time()}}
or passed to action constructors.
}
\description{
\code{ease()} creates custom easing functions by combining ease-in,
ease-out, and ease-time transformations. This allows building
sophisticated timing curves for animations.
}
\details{
Easing functions must satisfy:
\itemize{
\item Exactly one argument (the normalized time \code{t} in the closed
interval \[0, 1\])
\item Map 0 to 0 and 1 to 1 when evaluated at those endpoints
}
}
\examples{
# Linear easing (same as elinear)
e_linear <- ease()

# Ease-in with cubic
e_in <- ease(ein = ecubic)

# Ease-in-out cubic (default mode)
e_inout <- ease(ein = ecubic, eout = ecubic)

}
